#include<stdio.h>

int main()
{       /* This is the first Progam for the Hello World  --18/03/2025-- */
    printf("Hello World");                                                  /* This is used to produce the output to the screen */ 
    printf("\n This is the Second Line of code");                           /* Use of next Line Keyword */
    printf("\n the Size of the Integer in memory is: %d",sizeof(int));      /* Use of the Size of funtion/operator */
    printf("\n the Size of the float in memory is: %d",sizeof(float));
    printf("\n the Size of the string in memory is: %d",sizeof(char));
    printf("\n the Size of the long in memory is: %d",sizeof(long));
    return 0;                                                           /* If all ok return nothing ie 0 interger */

}